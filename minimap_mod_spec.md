# 汎用ミニマップMod 仕様書（AI実装用）

## 0. この文書について

本文書は、Minecraft Forge向け「サーバースクリプトで内容を操作可能な汎用ミニマップMod」を、AI（コーディングエージェント）に実装させるための仕様書である。MVPフェーズと、その後の拡張フェーズを分けて記述する。

具体的な想定用途：ValorantをMinecraft上で再現するゲームのミニマップ機能（スパイク位置・味方位置の表示）。

---

## 1. 対象環境

| 項目 | 内容 |
|---|---|
| Minecraft バージョン | 1.20.1 |
| Modローダー | Minecraft Forge 47.4.0 |
| 対象マップ規模 | 前提として、無限に広がるオープンワールド探索用ではなく、**ある程度範囲の定まったアリーナ/対戦マップ**を主用途とする（要確認：後述） |

---

## 2. アーキテクチャ概要

```
[データパック / mcfunction]
        │  カスタムコマンド呼び出し
        ▼
[サーバーサイド: Minimapコアシステム]
   ├─ Luaスクリプトエンジン（LuaJ埋め込み）
   ├─ MapState（アイコン・図形・エンティティ可視設定などの状態）
   └─ 同期マネージャ（差分検知 → パケット送信、チーム/プレイヤー単位でスコープ可）
        │  カスタムネットワークパケット（Forge SimpleChannel）
        ▼
[クライアントサイド: レンダラー]
   ├─ ベースマップ描画（俯瞰マップ画像 or 簡易ブロック色マップ）
   ├─ アイコン/図形オーバーレイ描画
   ├─ エンティティ表示制御の適用
   └─ 回転・オフセット処理
```

### 2.1 なぜこの構成か（決定事項の理由）

- **スクリプト言語：Lua（LuaJ）を採用**
  JS埋め込み（Rhino/GraalJS）は依存が重い、または言語仕様の癖（プロトタイプベース、非同期系構文）が学習コストを上げやすい。Luaは文法がシンプルで、ゲームスクリプティング用途（WoWアドオン、Roblox等）で「軽い学習コストでの外部スクリプト」として実績がある。LuaJは純JVM実装でネイティブライブラリ不要、Forge環境に組み込みやすい。
  → サンドボックス化した最小限のグローバルAPI（`Minimap.xxx`）のみを公開し、ファイルI/Oやネットワークアクセスは遮断する。

- **ミニマップ描画：スクラッチ実装を推奨**
  Xaero's MinimapやJourneyMapは「サーバースクリプトから毎フレーム/毎イベントで任意の図形・アイコンを描画し、特定エンティティの表示可否を動的制御する」ような汎用APIを公開していない（JourneyMapには経路点APIはあるが、図形描画やエンティティ表示制御までは対応しない）。要求される制御粒度を実現するには、既存Modへの統合よりスクラッチ実装の方が結果的にコストが低く、確実。
  想定マップがアリーナ規模であれば、チャンクキャッシュ等の重い最適化なしでも実用的な性能が出せるため、スクラッチのコストは許容範囲と判断する。
  ※もし将来「無限に広いワールドを探索するモード」も必要になった場合は、ベースマップ描画方式の見直しが必要（3.4項参照）。

---

## 3. サーバースクリプトAPI仕様（MVP）

### 3.1 スクリプトの配置とトリガー

- スクリプトファイルはワールドセーブ内 or データパックリソース内の専用フォルダ（例：`data/<namespace>/minimap_scripts/*.lua`）に配置。
- 呼び出し方法は2系統：
  1. **コマンド起動**：データパックの`.mcfunction`から `/minimap script run <script_id> [args...]` のように呼び出し、既存のコマンド駆動な運用（属性攻撃データパックと同様の使い勝手）と一貫させる。
  2. **イベントコールバック登録**：スクリプト内で `Minimap.onTick(fn)` 等を用い、サーバーTickやプレイヤー参加等のタイミングでLua関数を継続的に実行する。

### 3.2 公開API（MVP範囲）

```lua
-- アイコン表示
-- id: 一意な識別子（同じidで呼ぶと更新、removeIconで削除）
-- x, z: ワールド座標（yはオプション、階層フィルタ用に将来使用）
-- iconKey: 表示するアイコン画像の識別子（リソース側で事前登録）
-- opts: { scale=1.0, rotation=0, visibleTo="team:attacker" などのフィルタ条件 }
Minimap.setIcon(id, x, z, iconKey, opts)
Minimap.removeIcon(id)

-- 図形描画（円・矩形・線・ポリゴン等）
-- shapeType: "circle" | "rect" | "line" | "polygon"
-- points: 座標配列
-- style: { color="#FF0000", fillAlpha=0.3, lineWidth=2 }
Minimap.drawShape(id, shapeType, points, style)
Minimap.removeShape(id)

-- 回転・オフセット制御（プレイヤー単位 or 全体）
Minimap.setRotationMode(target, mode)   -- mode: "north_up" | "player_facing" | 固定角度
Minimap.setOffset(target, dx, dz)

-- エンティティ表示制御
-- target: エンティティ or プレイヤー参照
-- visible: true/false
-- overrideIconKey: 独自アイコンで上書き表示する場合に指定（省略可）
Minimap.setEntityVisibility(target, visible, overrideIconKey, opts)
```

`target`は概ね「プレイヤー」「チーム」「エンティティUUID」のいずれかを受け付ける形を想定（詳細は実装時に確定）。

### 3.3 同期スコープ

- MVPの用途（スパイク位置・味方位置）を踏まえ、**表示内容はプレイヤー単位、または陣営（バニラScoreboardチーム）単位でフィルタ可能**とする。
- サーバーは`MapState`の差分のみをクライアントへ送信し、無駄な帯域を避ける。

### 3.4 ベースマップ描画方式（要検討）

MVPでは以下いずれかを採用（実装コストと見た目のバランスで決定、AIへの実装指示時に明示すること）：
- (a) 上空からのブロック色を簡易サンプリングした静的画像を事前生成し、それをベースマップとして使用
- (b) 単色背景＋グリッド線程度の簡易表示（アリーナ用途なら十分な場合が多い）

無限ワールド探索を将来的にサポートする場合は、チャンク単位のタイル生成・キャッシュ方式（Xaero/JourneyMap同様の仕組み）への設計変更が必要になる点に注意。

---

## 4. MVP機能一覧

- [ ] 画面隅への常時ミニマップ表示（固定サイズ）
- [ ] Luaスクリプトからのアイコン表示・更新・削除（スパイク位置、味方プレイヤー位置）
- [ ] エンティティ表示制御（敵非表示、味方のみ表示など）
- [ ] 回転モード切り替え（北固定 / プレイヤー向き追従）とオフセット調整
- [ ] プレイヤー/チーム単位での表示内容フィルタリング

## 5. MVP後の拡張機能（優先順位順）

### フェーズ2：拡大表示
- 全画面 or 画面70%程度でのミニマップ表示への切り替え
- 挙動（拡大率、表示/非表示条件等）をサーバースクリプトである程度制御可能に
- クリック検知 → クリック座標（ワールド座標に変換）にピンを設置する機能
  - `Minimap.onMapClick(callback)` のようなコールバックAPIを追加想定

### フェーズ3：手元表示
- 画面中央付近に20〜30%程度でミニマップを表示
- 起動トリガー（アイテム使用、キーバインド等）は要確認（8項参照）
- クリック検知 → クリック座標をデータパック側から参照可能にする（例：スコアボード値やNBTストレージへの書き込み、または`function`呼び出しにイベントとして渡す）

### フェーズ4（優先度低）：表示位置調整
- ミニマップの画面内表示位置をサーバー側・クライアント側の双方から調整可能に
- サーバー指定とクライアント上書きの優先関係を要定義

### フェーズ5（将来）：天井透過
- 屋内MAP等でプレイヤーの真上の天井ブロックを透過し、内部を見せる機能
- コスト次第でMVPへの前倒しを再検討可（今回はMVP後送りで確定）

---

## 6. ネットワーク・パケット設計（概要）

- Forgeの`SimpleChannel`を用いたカスタムパケットで、`MapState`の差分をクライアントへ送信。
- パケット種別（例）：`IconUpdatePacket` / `IconRemovePacket` / `ShapeUpdatePacket` / `EntityVisibilityPacket` / `RotationOffsetPacket` / `MapClickPacket`（クライアント→サーバー、フェーズ2以降）
- 送信スコープはプレイヤー単位（対象プレイヤーにのみ送信）を基本とする。

---

## 7. 未確定・要確認事項（実装着手前に詰めるべき点）

1. **マップの想定スケール**：アリーナ規模限定か、将来的な無限ワールド探索対応も見据えるか（3.4項のベースマップ方式に直結）
2. **Modの識別子・パッケージ名**：既存プロジェクト（lynsmcdbd-mod）が `io.github.lyne324.<modid>` 形式のため、同様の命名で揃えるかどうか
3. **Luaサンドボックスの範囲**：無限ループ対策のタイムアウト、利用可能な標準ライブラリの制限範囲
4. **チーム管理の実装**：バニラのScoreboardチーム機能をそのまま使うか、独自データ構造を持つか
5. **アイコン画像リソースの供給方法**：データパック内蔵のリソースを参照するか、別途リソースパックを要求するか
6. **フェーズ3「手元表示」の起動トリガー**：専用アイテム使用、キーバインド、コマンドのいずれか（複数併用も可）
7. **クリック検知の座標変換精度**：フルスクリーンGUI上のクリック位置→ワールド座標への変換において許容される誤差範囲

---

## 8. 参考：想定ディレクトリ構成（例）

```
src/main/java/<package>/
├── ModMain.java
├── script/
│   ├── LuaScriptEngine.java
│   ├── MinimapLuaApi.java        // Lua側に公開するグローバル関数群
│   └── ScriptCommand.java        // /minimap script run 等のコマンド実装
├── state/
│   ├── MapState.java
│   └── MapStateSyncManager.java
├── network/
│   ├── NetworkHandler.java
│   └── packet/...
└── client/
    ├── MinimapOverlayRenderer.java
    ├── BaseMapRenderer.java
    └── IconShapeRenderer.java
```

（実装フェーズで詳細は変更可）
